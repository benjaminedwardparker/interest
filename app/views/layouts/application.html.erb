<!DOCTYPE html>
<html>
<head>
  <title>Pinterest</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body
  <% if !user_signed_in? %>
  style="margin: 0;background: url('http://i.imgur.com/H4e3dMi.jpg');background-size: 1440px 800px;"
  <% end %>
>
<div class="container">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <% if user_signed_in? %>
    <p>Glad to have you here, <%= current_user.email %>!</p>
    <%= button_to "Sign Out", destroy_user_session_path, method: "delete", class: "btn btn default", style: "float:right"%>
  <% end %>
</div>
<div class="container">


  <!-- Modal -->
<div class="modal fade" id="newPinModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div id="pin-form">
        <%= render partial: 'pins/form' %>
      </div>
    </div>
  </div>
</div>
<%= yield %>

</body>
</html>
